{% import '@SyliusAdmin/shared/helper/button.html.twig' as button %}

{{ button.primary(
    sylius_test_form_attribute('update-changes-button')|merge({
        text: 'sylius.ui.update'|trans,
        type: 'submit',
        form: 'odiseo_sylius_rbac_plugin_administration_role'
    })
) }}
