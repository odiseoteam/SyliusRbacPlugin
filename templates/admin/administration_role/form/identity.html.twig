{#
 # Wraps the two identity cards in a row so they sit side by side: `code` is a single short field
 # and the per-locale names need the width.
 #}
<div class="row g-3 mb-3">
    {% hook 'identity' %}
</div>
